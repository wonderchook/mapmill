hostname mapmill
vi /etc/hosts
mount /dev/xvdf /mnt
git clone git@github.com:wonderchook/mapmill
cd ~/mapmill/etc
apt-get update
apt-get install `cat doc/installed*`
/etc/init.d/postgresql stop
cp -a * /etc
sysctl -w kernel.shmmax=288997376
mv /var/lib/postgresql /mnt
ln -s /mnt/postgresql /var/lib/postgresql
/etc/init.d/postgresql start
su -c 'createuser -s ubuntu' postgres
createdb mapmill
ln -s ../sites-available/mapmill /etc/apache2/sites-enabled/
rm /etc/apache2/sites-enabled/000-default 
wget http://production.cf.rubygems.org/rubygems/rubygems-1.4.2.zip
unzip rubygems-1.4.2.zip 
cd rubygems-1.4.2/
ruby setup.rb
gem1.8 install bundler
bundle install
mv ~/mapmill /srv
cd /srv/mapmill
cp config/database.yml.example config/database.yml
rake db:migrate
/etc/init.d/apache2 restart
pip install mgrs
